#include <bits/stdc++.h>
using namespace std;
void minLateness(int n, vector<pair<int, pair<int, int>>> jobs)
{//                                  d
    sort(jobs.begin(), jobs.end());
    map<int, pair<int, int>> sch;
    int s = 0;
    for (auto it : jobs)
    {
        sch[]
    }
}
int main()
{
}